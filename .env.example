# Agent build — optional overrides (master deploy sets RELAY_URL)
# RELAY_URL=https://relay.example.workers.dev/agent
# AGENT_TARGETS=linux-x86_64,windows-x86_64
# LLVM_VERSION=22.1.0
# LLVM_INSTALL_DIR=/opt/llvm
